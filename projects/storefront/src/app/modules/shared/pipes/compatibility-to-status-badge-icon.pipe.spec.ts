import { CompatibilityToStatusBadgeIconPipe } from './compatibility-to-status-badge-icon.pipe';

describe('CompatibilityToStatusBadgeIconPipe', () => {
    it('create an instance', () => {
        const pipe = new CompatibilityToStatusBadgeIconPipe();
        expect(pipe).toBeTruthy();
    });
});
