import { CompatibilityToStatusBadgeTypePipe } from './compatibility-to-status-badge-type.pipe';

describe('CompatibilityToStatusBadgeTypePipe', () => {
    it('create an instance', () => {
        const pipe = new CompatibilityToStatusBadgeTypePipe();
        expect(pipe).toBeTruthy();
    });
});
