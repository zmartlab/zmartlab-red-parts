import { GetProductImagePipe } from './get-product-image.pipe';

describe('GetProductImagePipe', () => {
    it('create an instance', () => {
        const pipe = new GetProductImagePipe();
        expect(pipe).toBeTruthy();
    });
});
