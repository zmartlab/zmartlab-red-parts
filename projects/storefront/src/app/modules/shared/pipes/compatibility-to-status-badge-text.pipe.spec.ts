import { CompatibilityToStatusBadgeTextPipe } from './compatibility-to-status-badge-text.pipe';

describe('CompatibilityToStatusBadgeTextPipe', () => {
    it('create an instance', () => {
        const pipe = new CompatibilityToStatusBadgeTextPipe();
        expect(pipe).toBeTruthy();
    });
});
