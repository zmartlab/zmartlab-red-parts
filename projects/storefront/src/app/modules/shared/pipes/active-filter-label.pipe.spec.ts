import { ActiveFilterLabelPipe } from './active-filter-label.pipe';

describe('ActiveFilterLabelPipe', () => {
    it('create an instance', () => {
        const pipe = new ActiveFilterLabelPipe();
        expect(pipe).toBeTruthy();
    });
});
